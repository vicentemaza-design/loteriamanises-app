node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Firebase credentials — use VITE_FIREBASE_* env vars instead
firebase-applet-config.json

# Previews generados (artefactos de herramientas de diseño)
emails/templates/transaccional/*-preview.html

# Entregas empaquetadas
emails/delivery/

# Configuración local de herramientas
.agents/
.mcp.json

# Herramientas y salidas locales: nunca deben viajar en una entrega.
.vercel/
artifacts/

# Lo genera scripts/prepare-ios-startup.mjs en cada build, desde
# src/assets/brand/manises-isotipo.png, que sí está versionado.
public/startup/
